/* import { TableHeader, TableRowItem } from ".";
import styled from "./UserTable.module.scss";

const dummyArr = new Array(30).fill(0).map((_, idx) => 1 + idx);

const UsersSkeletonLoader = () => (
    <div className={`${styled.table} ${styled.skeleton__loader}`}>
        <TableHeader />
        {dummyArr.map((n) => (
            <TableRowItem
                key={n}
                dateJoined=""
                email=""
                org=""
                phoneNumber=""
                status=""
                username=""
            />
        ))}
    </div>
);

export default UsersSkeletonLoader;
 */

const Divvv = () => <div>lendsqr</div>;

export default Divvv;
