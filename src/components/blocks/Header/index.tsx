import styled from "./Header.module.scss";

const Header = () => (
    <header className={styled.header}>
        <div>
            <h3>header</h3>
        </div>
    </header>
);

export default Header;
